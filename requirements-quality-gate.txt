# Intentionally excludes torch, CUDA, transformers, and model packages.
# Capture/provenance tests decode images but never open a display, so the
# headless CPU OpenCV wheel is part of the reproducible offline lane.
Flask==3.1.3
numpy==2.4.6
opencv-python-headless==4.13.0.92
pandas==3.0.5
python-dotenv==1.2.2
requests==2.34.2
ruff==0.16.1
scipy==1.17.1
wordfreq==3.1.1
