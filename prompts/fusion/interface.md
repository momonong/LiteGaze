# 任務：進行 LexiGaze 專案「 perceber (感知) 」與「 cognition (認知) 」數據格式對齊與融合規格分析

我們即將為 LexiGaze 系統開發一個專屬的融合模組（Fusion Skill），用來將 `shengwen` 的眼動模組與 `weichi` 的文字難度模組進行後端數據整合。

在動手寫程式之前，請徹底掃描當前工作區（Workspace）的代碼，替我調查以下三個關鍵問題，並產出一份「數據融合藍圖報告」。

---

## 🔍 核心調查項目

### 1. 探索眼動模組 (`shengwen/` 或 `chenghao/`) 的數據落地格式
請幫我找到系統在記錄受試者閱讀眼動軌跡時，最終寫入本地的檔案（例如 `gaze_log.jsonl` 或 `raw.csv`）。請輸出：
- 該檔案的實際存放路徑規則。
- 實際範例資料（一整行 JSON 或幾筆 CSV 欄位）。
- 關鍵欄位確認：是否有包含時間戳記（`timestamp` 或 `duration`）？是否已經包含對齊好的單字字串（`word`），還是只有像素座標 `(x, y)`？

### 2. 探索認知模組 (`weichi/` 或 `text_model.py`) 的分數輸出格式
請幫我定位 BERT/LLM 計算出來的語言質量驚奇度權重檔（例如 `cognitive_weights.json`）。請輸出：
- 該檔案的 JSON 或字典結構（Schema）。
- 單字難度分數的 Key 名稱叫什麼？（例如是 `load_score`、`surprisal`、還是 `pll`？）
- 分數的數值區間範圍（例如是 0~1 之間，還是未歸一化的原始對數機率？）。

### 3. 定位 Flask 後端 (`server.py`) 的整合接入點
請幫我找出目前主要的 Flask 伺服器入口檔案。請評估：
- 我如果想要新增一個 `POST /api/fuse` 的後端藍圖（Blueprint）路由，應該在哪個檔案的哪一行進行 `app.register_blueprint()` 最安全且符合目前團隊的架構規範？

---

## 📊 預期輸出報告格式

請用繁體中文回傳一份結構化的 Markdown 報告，包含：
1. **【眼動感知端資料結構】**：路徑、欄位清單與範例。
2. **【語言認知端資料結構】**：路徑、欄位清單與範例。
3. **【對齊痛點評估】**：例如「單字大小寫是否需要統一處理？」、「眼動資料是否缺少時間差欄位？」等潛在 Bug 預警。
4. **【Flask 整合建議位置】**：修改的目標檔案與對應行數。